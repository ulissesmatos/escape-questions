// Ponto único de importação do Phaser (servido pelo Express a partir de
// node_modules/phaser — ver src/app.js). Trocar de versão = só o package.json.
export { default } from '/vendor/phaser/phaser.esm.min.js';
